// <auto-generated/>
#nullable enable
#pragma warning disable

namespace SteeleTerm.AddonModules.Updater
{
    partial class Update
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>&lt;Version&gt;(.*?)&lt;/Version&gt;</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.Compiled | RegexOptions.CultureInvariant</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match the string "&lt;Version&gt;".<br/>
        /// ○ 1st capture group.<br/>
        ///     ○ Match a character other than '\n' lazily any number of times.<br/>
        /// ○ Match the string "&lt;/Version&gt;".<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
        private static partial global::System.Text.RegularExpressions.Regex VersionRegex() => global::System.Text.RegularExpressions.Generated.VersionRegex_0.Instance;
    }
}

namespace SteeleTerm.Serial
{
    partial class SteeleTermSerial
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>\\((COM\\d+)\\)</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.IgnoreCase</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match '('.<br/>
        /// ○ 1st capture group.<br/>
        ///     ○ Match a character in the set [Cc].<br/>
        ///     ○ Match a character in the set [Oo].<br/>
        ///     ○ Match a character in the set [Mm].<br/>
        ///     ○ Match a Unicode digit atomically at least once.<br/>
        /// ○ Match ')'.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
        private static partial global::System.Text.RegularExpressions.Regex COM() => global::System.Text.RegularExpressions.Generated.COM_1.Instance;
    }
}

namespace SteeleTerm.Serial
{
    partial class SteeleTermSerial
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>VID_([0-9A-F]{4}).*PID_([0-9A-F]{4})</code><br/>
        /// Options:<br/>
        /// <code>RegexOptions.IgnoreCase</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match a character in the set [Vv].<br/>
        /// ○ Match a character in the set [Ii\u0130].<br/>
        /// ○ Match a character in the set [Dd].<br/>
        /// ○ Match '_'.<br/>
        /// ○ 1st capture group.<br/>
        ///     ○ Match a hexadecimal digit exactly 4 times.<br/>
        /// ○ Match a character other than '\n' greedily any number of times.<br/>
        /// ○ Match a character in the set [Pp].<br/>
        /// ○ Match a character in the set [Ii\u0130].<br/>
        /// ○ Match a character in the set [Dd].<br/>
        /// ○ Match '_'.<br/>
        /// ○ 2nd capture group.<br/>
        ///     ○ Match a hexadecimal digit exactly 4 times.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
        private static partial global::System.Text.RegularExpressions.Regex VIDPID() => global::System.Text.RegularExpressions.Generated.VIDPID_2.Instance;
    }
}

namespace System.Text.RegularExpressions.Generated
{
    using System;
    using System.Buffers;
    using System.CodeDom.Compiler;
    using System.Collections;
    using System.ComponentModel;
    using System.Globalization;
    using System.Runtime.CompilerServices;
    using System.Text.RegularExpressions;
    using System.Threading;

    /// <summary>Custom <see cref="Regex"/>-derived type for the VersionRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
    [SkipLocalsInit]
    file sealed class VersionRegex_0 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly VersionRegex_0 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private VersionRegex_0()
        {
            base.pattern = "<Version>(.*?)</Version>";
            base.roptions = RegexOptions.Compiled | RegexOptions.CultureInvariant;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 2;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 19 characters.
                    if (pos <= inputSpan.Length - 19)
                    {
                        // The pattern has the literal "<Version>" at the beginning of the pattern. Find the next occurrence.
                        // If it can't be found, there's no match.
                        int i = inputSpan.Slice(pos).IndexOfAny(Utilities.s_indexOfString_F8226DF7E1DBDCA5D9F2671E26154980ED2C5A954DFD787CBE201D221DD97D10);
                        if (i >= 0)
                        {
                            base.runtextpos = pos + i;
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    int capture_starting_pos = 0;
                    int lazyloop_capturepos = 0;
                    int lazyloop_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match the string "<Version>".
                    if (!slice.StartsWith("<Version>"))
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // 1st capture group.
                    //{
                        pos += 9;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos = pos;
                        
                        // Match a character other than '\n' lazily any number of times.
                        //{
                            lazyloop_pos = pos;
                            goto LazyLoopEnd;
                            
                            LazyLoopBacktrack:
                            UncaptureUntil(lazyloop_capturepos);
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            pos = lazyloop_pos;
                            slice = inputSpan.Slice(pos);
                            if (slice.IsEmpty || slice[0] == '\n')
                            {
                                UncaptureUntil(0);
                                return false; // The input didn't match.
                            }
                            pos++;
                            slice = inputSpan.Slice(pos);
                            lazyloop_pos = slice.IndexOfAny('\n', '<');
                            if ((uint)lazyloop_pos >= (uint)slice.Length || slice[lazyloop_pos] == '\n')
                            {
                                UncaptureUntil(0);
                                return false; // The input didn't match.
                            }
                            pos += lazyloop_pos;
                            slice = inputSpan.Slice(pos);
                            lazyloop_pos = pos;
                            
                            LazyLoopEnd:
                            lazyloop_capturepos = base.Crawlpos();
                        //}
                        
                        base.Capture(1, capture_starting_pos, pos);
                        
                        goto CaptureSkipBacktrack;
                        
                        CaptureBacktrack:
                        goto LazyLoopBacktrack;
                        
                        CaptureSkipBacktrack:;
                    //}
                    
                    // Match the string "</Version>".
                    if (!slice.StartsWith("</Version>"))
                    {
                        goto CaptureBacktrack;
                    }
                    
                    // The input matched.
                    pos += 10;
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                    
                    // <summary>Undo captures until it reaches the specified capture position.</summary>
                    [MethodImpl(MethodImplOptions.AggressiveInlining)]
                    void UncaptureUntil(int capturePosition)
                    {
                        while (base.Crawlpos() > capturePosition)
                        {
                            base.Uncapture();
                        }
                    }
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the COM method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
    [SkipLocalsInit]
    file sealed class COM_1 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly COM_1 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private COM_1()
        {
            base.pattern = "\\((COM\\d+)\\)";
            base.roptions = RegexOptions.IgnoreCase;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 2;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 6 characters.
                    if (pos <= inputSpan.Length - 6)
                    {
                        // The pattern matches a character in the set [Mm] at index 3.
                        // Find the next occurrence. If it can't be found, there's no match.
                        ReadOnlySpan<char> span = inputSpan.Slice(pos);
                        for (int i = 0; i < span.Length - 5; i++)
                        {
                            int indexOfPos = span.Slice(i + 3).IndexOfAny('M', 'm');
                            if (indexOfPos < 0)
                            {
                                goto NoMatchFound;
                            }
                            i += indexOfPos;
                            
                            if (((span[i + 1] | 0x20) == 'c') &&
                                ((span[i + 2] | 0x20) == 'o'))
                            {
                                base.runtextpos = pos + i;
                                return true;
                            }
                        }
                    }
                    
                    // No match found.
                    NoMatchFound:
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    int capture_starting_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match '('.
                    if (slice.IsEmpty || slice[0] != '(')
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // 1st capture group.
                    {
                        pos++;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos = pos;
                        
                        if ((uint)slice.Length < 3 ||
                            !slice.StartsWith("com", StringComparison.OrdinalIgnoreCase)) // Match the string "com" (ordinal case-insensitive)
                        {
                            UncaptureUntil(0);
                            return false; // The input didn't match.
                        }
                        
                        // Match a Unicode digit atomically at least once.
                        {
                            pos += 3;
                            slice = inputSpan.Slice(pos);
                            int iteration = 0;
                            while ((uint)iteration < (uint)slice.Length && char.IsDigit(slice[iteration]))
                            {
                                iteration++;
                            }
                            
                            if (iteration == 0)
                            {
                                UncaptureUntil(0);
                                return false; // The input didn't match.
                            }
                            
                            slice = slice.Slice(iteration);
                            pos += iteration;
                        }
                        
                        base.Capture(1, capture_starting_pos, pos);
                    }
                    
                    // Match ')'.
                    if (slice.IsEmpty || slice[0] != ')')
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // The input matched.
                    pos++;
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                    
                    // <summary>Undo captures until it reaches the specified capture position.</summary>
                    [MethodImpl(MethodImplOptions.AggressiveInlining)]
                    void UncaptureUntil(int capturePosition)
                    {
                        while (base.Crawlpos() > capturePosition)
                        {
                            base.Uncapture();
                        }
                    }
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the VIDPID method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
    [SkipLocalsInit]
    file sealed class VIDPID_2 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly VIDPID_2 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private VIDPID_2()
        {
            base.pattern = "VID_([0-9A-F]{4}).*PID_([0-9A-F]{4})";
            base.roptions = RegexOptions.IgnoreCase;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 3;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // Search until we can't find a valid starting position, we find a match, or we reach the end of the input.
                    while (TryFindNextPossibleStartingPosition(inputSpan) &&
                           !TryMatchAtCurrentPosition(inputSpan) &&
                           base.runtextpos != inputSpan.Length)
                    {
                        base.runtextpos++;
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Any possible match is at least 16 characters.
                    if (pos <= inputSpan.Length - 16)
                    {
                        // The pattern matches a character in the set _ at index 3.
                        // Find the next occurrence. If it can't be found, there's no match.
                        ReadOnlySpan<char> span = inputSpan.Slice(pos);
                        for (int i = 0; i < span.Length - 15; i++)
                        {
                            int indexOfPos = span.Slice(i + 3).IndexOf('_');
                            if (indexOfPos < 0)
                            {
                                goto NoMatchFound;
                            }
                            i += indexOfPos;
                            
                            if (((span[i] | 0x20) == 'v') &&
                                ((span[i + 2] | 0x20) == 'd'))
                            {
                                base.runtextpos = pos + i;
                                return true;
                            }
                        }
                    }
                    
                    // No match found.
                    NoMatchFound:
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int capture_starting_pos = 0;
                    int capture_starting_pos1 = 0;
                    int charloop_capture_pos = 0;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    if ((uint)slice.Length < 4 ||
                        ((slice[0] | 0x20) != 'v') || // Match a character in the set [Vv].
                        ((((ch = slice[1]) | 0x20) != 'i') & (ch != 'İ')) || // Match a character in the set [Ii\u0130].
                        !slice.Slice(2).StartsWith("d_", StringComparison.OrdinalIgnoreCase)) // Match the string "d_" (ordinal case-insensitive)
                    {
                        UncaptureUntil(0);
                        return false; // The input didn't match.
                    }
                    
                    // 1st capture group.
                    {
                        pos += 4;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos = pos;
                        
                        // Match a hexadecimal digit exactly 4 times.
                        {
                            if ((uint)slice.Length < 4 ||
                                !char.IsAsciiHexDigit(slice[0]) ||
                                !char.IsAsciiHexDigit(slice[1]) ||
                                !char.IsAsciiHexDigit(slice[2]) ||
                                !char.IsAsciiHexDigit(slice[3]))
                            {
                                UncaptureUntil(0);
                                return false; // The input didn't match.
                            }
                        }
                        
                        pos += 4;
                        slice = inputSpan.Slice(pos);
                        base.Capture(1, capture_starting_pos, pos);
                    }
                    
                    // Match a character other than '\n' greedily any number of times.
                    //{
                        charloop_starting_pos = pos;
                        
                        int iteration = slice.IndexOf('\n');
                        if (iteration < 0)
                        {
                            iteration = slice.Length;
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                        
                        charloop_ending_pos = pos;
                        goto CharLoopEnd;
                        
                        CharLoopBacktrack:
                        UncaptureUntil(charloop_capture_pos);
                        
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (charloop_starting_pos >= charloop_ending_pos ||
                            (charloop_ending_pos = inputSpan.Slice(charloop_starting_pos, charloop_ending_pos - charloop_starting_pos).LastIndexOfAny('P', 'p')) < 0)
                        {
                            UncaptureUntil(0);
                            return false; // The input didn't match.
                        }
                        charloop_ending_pos += charloop_starting_pos;
                        pos = charloop_ending_pos;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd:
                        charloop_capture_pos = base.Crawlpos();
                    //}
                    
                    if ((uint)slice.Length < 4 ||
                        ((slice[0] | 0x20) != 'p') || // Match a character in the set [Pp].
                        ((((ch = slice[1]) | 0x20) != 'i') & (ch != 'İ')) || // Match a character in the set [Ii\u0130].
                        !slice.Slice(2).StartsWith("d_", StringComparison.OrdinalIgnoreCase)) // Match the string "d_" (ordinal case-insensitive)
                    {
                        goto CharLoopBacktrack;
                    }
                    
                    // 2nd capture group.
                    {
                        pos += 4;
                        slice = inputSpan.Slice(pos);
                        capture_starting_pos1 = pos;
                        
                        // Match a hexadecimal digit exactly 4 times.
                        {
                            if ((uint)slice.Length < 4 ||
                                !char.IsAsciiHexDigit(slice[0]) ||
                                !char.IsAsciiHexDigit(slice[1]) ||
                                !char.IsAsciiHexDigit(slice[2]) ||
                                !char.IsAsciiHexDigit(slice[3]))
                            {
                                goto CharLoopBacktrack;
                            }
                        }
                        
                        pos += 4;
                        slice = inputSpan.Slice(pos);
                        base.Capture(2, capture_starting_pos1, pos);
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                    
                    // <summary>Undo captures until it reaches the specified capture position.</summary>
                    [MethodImpl(MethodImplOptions.AggressiveInlining)]
                    void UncaptureUntil(int capturePosition)
                    {
                        while (base.Crawlpos() > capturePosition)
                        {
                            base.Uncapture();
                        }
                    }
                }
            }
        }

    }
    
    /// <summary>Helper methods used by generated <see cref="Regex"/>-derived implementations.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.23019")]
    file static class Utilities
    {
        /// <summary>Default timeout value set in <see cref="AppContext"/>, or <see cref="Regex.InfiniteMatchTimeout"/> if none was set.</summary>
        internal static readonly TimeSpan s_defaultTimeout = AppContext.GetData("REGEX_DEFAULT_MATCH_TIMEOUT") is TimeSpan timeout ? timeout : Regex.InfiniteMatchTimeout;
        
        /// <summary>Whether <see cref="s_defaultTimeout"/> is non-infinite.</summary>
        internal static readonly bool s_hasTimeout = s_defaultTimeout != Regex.InfiniteMatchTimeout;
        
        /// <summary>Supports searching for the string "&lt;Version&gt;".</summary>
        internal static readonly SearchValues<string> s_indexOfString_F8226DF7E1DBDCA5D9F2671E26154980ED2C5A954DFD787CBE201D221DD97D10 = SearchValues.Create(["<Version>"], StringComparison.Ordinal);
    }
}
